// constants.dart
import 'package:flutter/material.dart';

// Primary Theme Colors
const Color themeYellow = Color(0xFFFCBF02); // Main yellow accent
const Color darkText = Color(0xFF1A237E); // For dark text (navs, labels)
const Color darkBlue = Color(0xFF0C2461);
const Color lightGray = Color(0xFFF6F6F6);
const Color lightBackground = Colors.white; // General background
const Color accentBrown = Colors.brown; // Buttons, AppBar

// Font
const String primaryFont = 'AppleSDGothicNeo';

// App-wide Padding
const double defaultPadding = 16.0;
